{{> navigation}}
{{> breadcrumb}}
<section>
    <div class="container">
        <div class="col-md-12">
            <div class="content">
                <h1><span>Register</span></h1>
                <form method="post" novalidate="" class="form-horizontal"><input type="hidden" name="_csrf" value="QTNJXy1NdyPPThtl5+b24rvcqvi/wnv3wtLcs=">
                    <fieldset>
                        <legend>(*Required)</legend>
                        <!-- Button Drop Down -->
                        <div class="form-group">
                            <div class="col-md-5"><input id="name.first" class="form-control input-md" name="name.first" required="" type="text" placeholder="First Name*" /></div>
                            <div class="col-md-5"><input id="name.last" class="form-control input-md" name="name.last" required="" type="text" placeholder="Last Name*" /></div>
                        </div>
                        <!-- Text input-->
                        <div class="form-group">
                            <div class="col-md-10"><input id="email" class="form-control input-md" name="email" type="text" placeholder="Email Address*" /></div>
                        </div>
                        <div class="form-group"><input type="password" name="password" placeholder="password" id="password" class="form-control"></div>
                        <div class="form-group"><div class="col-md-4"><button type="submit" id="submit-form" class="btn btn-lead btn-lg">Submit</button></div></div>
                    </fieldset>
                </form>
                <div class="confirmationMessage" style="display:none;">
                    <hr>
                    {{#if data.form.confirmationMessage}}
                        {{{data.form.confirmationMessage}}}
                    {{else}}
                        <p>Thanks for contacting us. We will get back to you as soon as possible.</p>
                    {{/if}}
                </div>
                <input type="hidden" id="user" value="{{data.user.id}}"/>
            </div>
        </div>
    </div>
    </div>
</section>
<script type="text/javascript">
    $(function () {
        $('.content form').on('click', '#submit-form', function (e) {
            e.preventDefault();

            $('form').parsley().validate();
            if ($('form').parsley().isValid() === true) {

            }
        });

        $('.datetimepicker').datetimepicker();

        $('.input-group-btn ul li a').click(function (e) {
            e.preventDefault();
            var $this = $(this);
            var currVal = $this.parents('.input-group').find('input').val();
            $this.parents('.input-group').find('input').val(currVal + ' ' + $this.text());
        });
    });
</script>
